# US-006: Health metrics cards
# Verifies health cards display on Health tab

appId: com.likenootter.app

---
- launchApp

- assertVisible:
    id: "app-ready"

# Navigate to Health tab
- tapOn:
    text: "Health"

# Verify health metric cards are visible
- assertVisible:
    id: "sleep-card"

- assertVisible:
    id: "steps-card"

- assertVisible:
    id: "calories-card"

- assertVisible:
    id: "rhr-card"
